inputs:
- tests-examples/spider/flight_4/tables/airports.csv
output: tests-examples/spider/flight_4/tables/0028.csv
constants:
- '2'
functions:
- count
comment: |-
  SELECT city
  FROM airports
  GROUP BY city
  HAVING count(*) > 2
  ORDER BY count(*)
