inputs:
- tests-examples/spider/cinema/tables/cinema.csv
output: tests-examples/spider/cinema/tables/0021.csv
constants:
- '300'
- '1'
functions:
- count
columns:
- capacity
comment: |-
  SELECT LOCATION
  FROM cinema
  WHERE capacity > 300
  GROUP BY LOCATION
  HAVING count(*) > 1
