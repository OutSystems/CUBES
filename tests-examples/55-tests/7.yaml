inputs:
  - tests-examples/55-tests/tables/7-1.txt
output: tests-examples/55-tests/tables/7.out
aggrs:
  - mean
attrs:
  - age
loc: 1
comment: |
  5.1.7 optimal solution

  df1 <- input1 %>% group_by(level) %>% summarise(meanage = mean(age)) %>% ungroup()
  out <- df1 %>% select(level,meanage) %>% distinct()
