inputs:
  - tests-examples/55-tests/tables/17-1.txt
  - tests-examples/55-tests/tables/17-3.txt
output: tests-examples/55-tests/tables/53.out
aggrs:
  - min
attrs:
  - cost
bools:
  - ==
loc: 4
comment: |
  5.2.5 optimal solution

  df1 <- input1 %>% group_by(P_id) %>% summarise(mincost = min(cost)) %>% ungroup()
  df2 <- inner_join(inner_join(df1, input1), input2)
  df3 <- df2 %>% filter(mincost == cost)
  out <- df3 %>% select(P_id, S_name) %>% distinct()
