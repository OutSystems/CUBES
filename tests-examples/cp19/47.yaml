inputs:
- tests-examples/cp19/tables/8-1.txt
output: tests-examples/cp19/tables/8.out
const:
- JR
aggrs:
- mean
attrs:
- level
- age
bools:
- '!='
loc: '5'
comment: "# 5.1.8 optimal solution\n\nstudent %>% group_by(level) %>% summarize(average=mean(age))\
  \ %>% \nfilter(level != \"JR\")"
