inputs:
- tests-examples/textbook/tables/12-1.txt
output: tests-examples/textbook/tables/12.out
const: []
aggrs:
- n
- max(n)
attrs:
- n
bools:
- ==
loc: '2'
comment: "# 5.1.12 optimal solution\n\nstudent %>% group_by(age,level) %>% summarise(n\
  \ = n()) %>% \nfilter(n==max(n)) %>% select(-n)"
