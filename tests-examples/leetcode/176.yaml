inputs:
  - tests-examples/leetcode/tables/176.csv
output: tests-examples/leetcode/tables/176_o.csv
aggrs:
  - max
filters:
  - max(Salary)
attrs:
  - Salary
comment:
  df2 <- input1 %>% group_by(Id, Salary) %>% summarise(maxSalary = max(Salary)) %>% ungroup()
  df3 <- df2 %>% filter(maxSalary >= Salary & Salary != max(Salary))
  df4 <- df3 %>% filter(maxSalary >= Salary & maxSalary == max(Salary))
  out <- df4 %>% select(SecondHighestSalary = Salary)

  Weird solution?
