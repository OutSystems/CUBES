inputs:
  - tests-examples/scythe/top_rated_posts/tables/i040.csv
output: tests-examples/scythe/top_rated_posts/tables/o040.csv
aggrs:
  - n
comment: |
  SELECT a, b, COUNT(a)
  FROM tbl
  GROUP BY a, b

  df1 <- input1 %>% group_by(group, subGroup) %>% summarise(n = n())
  out <- df1 %>% ungroup() %>% select(group,subGroup,n)